# 2026-05-03

## 修复编译错误 - NavigationView.swift

- **文件**: `Sources/Shared/Views/Core/NavigationView.swift`
- **问题 1 (L59)**: `KMStore.ToolItem` 没有 `healthCheck` 成员，将 `.tool(.lint), .tool(.healthCheck)` 简化为 `.tool(.lint)`
- **问题 2 (L168)**: `let hasResult = store.synthesisResults[type] != nil` 声明后从未使用，删除该行
- **根因**: `ToolItem` 枚举成员为 `index, chat, log, lint, tagCloud, collab, taskCenter, weeklyReport, dashboard, pluginMarket, synthesis`，无 `healthCheck`

## 编译构建结果

- **KM (iOS)**: ✅ BUILD SUCCEEDED
- **KMMac**: ✅ BUILD SUCCEEDED
- **KMWatch**: ❌ 无法构建 —— 环境问题，watchOS 26.4 平台未安装（需 Xcode > Settings > Components 下载）
- 额外修复: `NavigationView.swift` 中 `.aiScan` 已被重命名为 `.taskCenter`，顺手修复

## 修复图谱Tab页显示问题

- **问题**: 图谱 Tab 页内导航栏的 segmented picker（2D/3D 切换）显示为原始 key 字符串 "graph.mode.2d" / "graph.mode.3d"
- **根因**: `Localizable.xcstrings` 中缺少 `"graph.mode.2d"` 和 `"graph.mode.3d"` 的本地化定义，`Localized.tr()` 回退显示 key 本身
- **修复**: 在 `Localizable.xcstrings` 中添加中英文翻译
  - `"graph.mode.2d"` → `"2D"` / `"2D 图谱"`
  - `"graph.mode.3d"` → `"3D"` / `"3D 图谱"`

## 修复图谱2D/3D切换布局 + 移除3D返回按钮

- **问题 1**: 2D/3D segmented picker 在 `GraphContainerView` 的外层 ZStack 上，切到 3D 时 Graph3DView 会隐藏导航栏但 toolbar 仍尝试渲染，导致布局冲突
  - **修复**: 将 `.toolbar`、`.navigationTitle`、`.navigationBarTitleDisplayMode` 移到 `if selectedTab == .graph2D` 分支内部
- **问题 2**: Graph3DView 顶部有自定义返回按钮（`backButton`）覆盖层
  - **修复**: 删除 `@Environment(\.dismiss)`、`backButton` overlay 和 `backButton` 计算属性（3D 视图通过主 segmented picker 切换退出）
- **文件**: `GraphView.swift`、`Graph3DView.swift`

## 修复图谱Tab页按钮双份 + 2D/3D控件下移

- **问题 1**: 右上角节点统计/洞察按钮显示双份（`.toolbar` 在 if 分支内外各声明一次，SwiftUI 合并后渲染两份）
  - **修复**: 删除 if 分支内的 `.toolbar`、`.navigationTitle`、`.navigationBarTitleDisplayMode`，只保留外层单一声明
- **问题 2**: 2D 图谱的缩放控件、筛选药丸被导航栏遮挡，需要下移到 Tab picker 下方
  - **修复**: `GraphZoomControlsView` 添加 `.padding(.top, 100)`；`GraphFilterPillsView` 外层用 VStack 包住确保从顶部开始；调整 2D 内 overlay 顺序（先 filter pills 再 zoom controls）
- **问题 3**: 3D 图谱 controls overlay 定位在 top: 36，离 Tab picker 太近
  - **修复**: `Graph3DView.swift` 中 controlsOverlay 的 `.padding(.top, 36)` → `.padding(.top, 120)`
- **文件**: `GraphView.swift`、`Graph3DView.swift`

## 将图谱统计+洞察按钮从 toolbar 移到 Tab 下方

- **问题**: "3 节点 · 3 连接" + 洞察灯泡按钮在导航栏 toolbar 右侧，与 2D/3D Tab picker 同行，应移到 Tab 下方
- **修复**:
  1. 从 `.toolbar` 中移除 `graphToolbar`，toolbar 只保留 segmented picker
  2. 将 `graphToolbar`（`ToolbarContent`）改为 `graphStatsBar`（普通 `View`）
  3. 在 2D 图谱 ZStack 内顶部添加 `graphStatsBar` 作为 overlay，位于筛选药丸上方
- **文件**: `GraphView.swift`

## 修复2D控件重叠 + 3D默认全屏问题

- **问题 1**: 2D 图谱统计栏和筛选药丸各自用 VStack+Spacer 定位，导致两者重叠
  - **修复**: 将 `graphStatsBar` 和 `GraphFilterPillsView` 合并为一个 VStack，统一管理间距
  - **简化**: `GraphFilterPillsView` 移除内部多余的 VStack+Spacer，直接返回 ScrollView
- **问题 2**: 3D 图谱默认全屏，且将导航栏 Tab 也渲染进去了
  - **修复 1**: 移除外层 `.ignoresSafeArea(edges: .bottom)`
  - **修复 2**: `controlsOverlay` padding 改为动态值 `isFullScreen ? 40 : 8`
  - **修复 3**: `Graph3DComponents.swift` 中 `Graph3DControlsOverlay` 移除多余的 `.padding(.top, 8)` 和 `.padding(.trailing, 16)`（外层已处理）
- **文件**: `GraphView.swift`、`Graph3DView.swift`、`Graph3DComponents.swift`

## 修复筛选药丸右对齐无响应 + 3D默认全屏

- **问题 1**: 筛选药丸右对齐，且点击按钮无反应
  - **根因**: 外层 VStack 有 `Spacer()` 把筛选药丸挤到底部去了，导致按钮在屏幕外且点击区域错位
  - **修复**: 删除 VStack 末尾的 `Spacer()`，让 VStack 紧贴顶部排列
- **问题 2**: 3D 图谱默认全屏（`isFullScreen = false`），导致导航栏被覆盖
  - **修复**: `isFullScreen` 默认值改为 `true`，保持 3D 内容在安全区域内正常显示
- **文件**: `GraphView.swift`、`Graph3DView.swift`

## 修复启动默认进入知识索引而非知识库主页

- **根因**: 代码中多处 `splashDismissed` 和 `onAppear` 强制将 `sidebarSelection` 设为 `.tool(.index)`（知识索引），用户实际想进入的是知识库主页 `KnowledgeDashboardView`
- **涉及文件**:
  - `ContentView.swift` — `@State sidebarSelection` 初始值、`onAppear`、`splashDismissed` 均改为 `.tool(.dashboard)`
  - `SidebarView.swift` — `onAppear` 和 `splashDismissed` 处理改为 Dashboard
  - `NavigationView.swift` — `@State selection` 初始值、`onAppear`、`splashDismissed` 均改为 `.tool(.dashboard)`
- **知识库 Tab vs 知识索引页面区别**:
  - `.wiki` Tab = 底部导航「知识库」主 tab（图标 books.vertical.fill）
  - 侧边栏 `.tool(.index)` = **知识索引**页面（IndexView，按时间/类型列出所有页面）
  - 侧边栏 `.tool(.dashboard)` = **知识库主页**（KnowledgeDashboardView，统计概览）
